# 游戏脚本目录（scripts）

本目录用于存放游戏运行所需的 JavaScript 脚本（`.js`）。

- 与安卓版一致，**仓库默认不预置任何脚本**，需要自行导入。
- 导入方式：在 App 内「脚本」页面通过文件导入，或从电脑把 `.js` 文件放进此目录后重新打包。
- **`00-省电模式修复.js` 为常驻必需脚本**，不导入会导致游戏在后台/锁屏时异常耗电或被系统回收，请务必导入并保持开启。

> 注意：本目录必须保留（含此 README），否则 XcodeGen 生成工程时会因缺少 source 目录而失败。
